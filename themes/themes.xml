<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UIDesigns" parent="Theme.Material3.DayNight.NoActionBar"/>
    <style name="Theme.UIDesigns" parent="Base.Theme.UIDesigns" />
    <style name="collapseModeTheme">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="expandedModeTheme">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Bottom Nav indicator Theme -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:width">56dp</item>
        <item name="android:height">56dp</item>
        <item name="android:color">#FF9800</item>
    </style>

</resources>